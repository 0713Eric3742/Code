#include<bits/stdc++.h>
using namespace std;
int c[100005]={};
int main()
{
	ios::sync_with_stdio(false);
	cin.tie(0);
	int a,b,d,e;
	vector<int> ans;
	cin>>a;
	for (int i = 0; i < a; i++)
	{
		cin>>c[i];
	}
	cin>>b;
	for (int i = 0; i < b; i++)
	{
		cin>>d>>e;
		ans.clear();
		for (int j = d; j <= e; j++)
		{
			if(j%2==0)
			if(j%2==0)
		
	}
	
	return 0;
}